# dir_path=$( cd $( dirname "$BASH_SOURCE" ) && pwd )
# cd "$dir_path"
# git config --global user.email "tisutoo@gmail.com"
# git config --global user.name "seohasong"
# [ -d ~/.ssh ] && rm -r ~/.ssh
# mkdir ~/.ssh
# cp id_rsa* ~/.ssh
# chmod 600 ~/.ssh/*
# cd "${dir_path%/*}"
# unset dir_path
# rm -rf KEYS
# git clone git@github.com:SeohaSong/SEOHASONG.git
# . SEOHASONG/install.sh
